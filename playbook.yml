---

#---------------------------------
# 1. Prepare System
#---------------------------------
- hosts:
    - all_nodes
  tasks:
    - include_role: name=system
      when: install_system

#---------------------------------
# 2. Install JDK
#---------------------------------
- hosts:
    - all_nodes
  tasks:
    - include_role: name=jdk
      when: install_java_sdk

#---------------------------------
# 3. Install Coordinator
#---------------------------------


#---------------------------------
# 4. Install Executor
#---------------------------------

#---------------------------------
# 5. Install Driver Proxy
#---------------------------------
